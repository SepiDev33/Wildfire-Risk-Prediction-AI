# AI Search Project

## Wildfire Risk Prediction Using Machine Learning

This project explores the use of machine learning to predict wildfire risk using environmental data.

The repository contains:

- `dataset/` – information about datasets used
- `code/` – implementation of the wildfire prediction model
- `performance/` – evaluation metrics and results
- `report/` – proposal and written project documents
